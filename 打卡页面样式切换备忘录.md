# 打卡页面样式切换备忘录

记录日期：2026-09-10

状态：首版已实现，详见下方进展；后续设想仍按文中建议推进。

## 计划

制作多个不同视觉样式的打卡页面，由后台自由选择当前使用的样式。员工仍通过原有打卡地址或二维码进入，无须记住不同页面地址。重点考虑切换过程的连续性，避免影响员工选择、打卡提交和成功反馈。

首批可考虑：简洁实用版、年轻和服卡通版、其他后续确定的样式。当前 `attendance/demo/` 仅为独立演示，使用临时内存记录，尚未接入正式考勤接口。

## 实现方向

- **统一业务，替换外观。** 各样式共用员工选择、签到签退、状态查询、错误提示和多语言逻辑；样式负责布局、配色、人物及动画。不能每做一套页面就复制一套考勤逻辑。
- **固定入口。** 保持 `/` 及打卡二维码目标地址稳定，由系统根据后台配置加载对应样式。用稳定的样式标识和资源版本管理页面，不接受后台任意输入模板路径。
- **后台选择。** 提供样式缩略图、名称、设备适配说明和独立预览，明确区分“预览”与“应用”。应用成功后显示当前样式，并可切回上一版。
- **统一状态来源。** 正式打卡结果以服务器记录为准，只有确认写入成功后才播放成功动画。人物、动画加载失败不得阻断打卡。

## 无缝切换：建议先明确生效规则

优先采用“新进入页面立即生效，已打开页面在安全时机切换”的规则。这里的无缝首先指地址、业务状态和操作不中断，不要求首版就实现复杂的页面热替换。

| 页面当前状态 | 建议处理 |
| --- | --- |
| 新打开或重新进入 | 加载后台当前已启用的样式 |
| 空闲，未选择员工 | 新样式资源准备好后切换；失败则保留当前页面 |
| 已选择员工，未提交 | 保留员工选择，切换后重新查询服务器状态，避免错误显示签到或签退按钮 |
| 正在提交或排队 | 暂缓切换，先完成请求和结果确认；不得因切换重发打卡请求 |
| 请求超时，结果未知 | 先核实服务器是否已记录，再恢复操作或切换；需设计请求标识和防重复机制 |
| 成功提示或鞠躬动画正在展示 | 保留成功时间，等待提示关闭或动画结束后再切换；动画结束不等于业务完成 |
| 已签退 | 保留已完成状态，不能切换后又出现可重复打卡的按钮 |

如果第一版采用安全时机刷新页面，应只短暂保留必要的员工选择信息，进入新页面后重新校验员工有效性与当天状态；不能把客户端保存的“成功”当成真实记录。共享打卡设备还应考虑完成后清除员工选择，避免下一人误打。

## 需要重点设计的细节

1. **变更通知与一致性：** 后台保存样式标识及配置版本，页面以适当频率检查更新，或在重新获得焦点时检查。离线页面恢复连接后应重新核对配置；连续多次修改时只采用最新有效版本。
2. **资源预加载与回退：** Three.js、模型和纹理可能较大。切换前准备必要资源，资源使用版本化地址避免混用旧缓存；超时、缺失或 WebGL 不可用时提供基础界面。避免先清空旧页面再等待新页面。
3. **切换边界：** 切换时清理旧页面计时器、事件监听、动画循环和三维资源，避免重复提交、内存增长或旧界面继续更新。避免两套样式的 CSS 相互影响。
4. **设备与语言：** 同一样式需适配电脑、平板和手机；保持语言及必要偏好，支持减少动态效果。页面切换后恢复合理焦点，不能用过渡动画遮住主要操作。
5. **权限与发布：** 样式应用接口必须验证管理员权限，不能仅靠隐藏后台入口。预览不改变全局设置、不写入正式考勤。配置更新应可追溯、可回退，业务数据不随样式回退。

## 分阶段推进建议

1. 先整理共用打卡逻辑，选两套样式，完成后台预览、应用及固定入口加载。
2. 做好重新进入页面时的样式生效、资源失败回退和业务状态恢复。
3. 再实现已打开页面的配置感知与安全时机切换；确有需要时才做不刷新页面的动态替换。

## 后续需确定

- 样式是全公司统一，还是允许按门店或设备分别设置？建议首版全局统一。
- 后台改样式后，已打开页面是否需要自动跟随？建议空闲时自动切换，业务处理中延后。
- 是否支持用户自行上传样式？建议首版只提供随程序发布、已验证的内置样式。

## 验收关注点

保持原有地址和二维码可用；切换前后员工和当天状态正确；提交中切换不丢单、不重复打卡；成功提示不会被清掉；样式或三维资源失败仍能完成基础打卡；重复切换不累积动画和监听器；手机、多语言和后台回退均可正常使用。


## 2026-09-10 首版进展

- 固定 `/` 入口支持 11 种样式：经典原版、霓虹科幻、和服卡通、极光漫游、森间呼吸、深海蓝调、山间日出、今日票根、像素街机、糖果气泡、极简黑白；每款均有打卡成功动画。经典原版从 Git 历史恢复外观，并接入共用控制器。后台 `/admin/appearance` 提供独立试打卡预览、应用、版本冲突检测和恢复上一样式。
- 全部正式页面使用共用控制器与考勤接口；原 `attendance/demo/` 保留为独立的早期演示。
- 空闲页面约每 8 秒检查配置，资源准备后刷新到新样式；短暂保存员工选择与成功提示，重新查询服务器当天状态。提交、结果核实或弹窗期间延后切换。
- 新增持久化打卡请求标识，重复发送同一请求不会重复排队；断线后查询请求结果，服务重启后核实记录。保留原打卡接口供已有客户端使用。
- 当前没有完整管理员账号体系，因此新样式管理只允许本机访问，并校验会话令牌及请求主机。远程管理、按门店配置、上传样式与无刷新动态替换尚未实现。
- 浏览器试用使用独立数据库 `/tmp/attendance-theme-trial`，与正式用户数据分离。运行方式仍遵循 DEVELOPMENT.md；模板和静态资源随原有打包流程包含。
