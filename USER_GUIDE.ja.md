<h1 align="center">📖 勤怠管理システム 使用説明</h1>

<h2 align="center"><a href="README.ja.md">ダウンロードページへ戻る</a></h2>

この説明では各機能の使い方を紹介します。初めて使うときは、順番に設定してください。

## 1. Windows：ダウンロード、展開、起動、終了

1. GitHub の v1.0.3 リリースページから Windows の ZIP をダウンロードします。
2. 「ダウンロード」フォルダーで ZIP を右クリックし、「すべて展開」を選んで展開します。
3. 展開したフォルダーを開き、中の `.exe` をダブルクリックします。ZIP のプレビューから直接起動しないでください。
4. 「Windows によって PC が保護されました」と表示されたら、公開ページから取得したことを確認して「詳細情報 → 実行」を選びます。
5. ブラウザが開くまで待ちます。開かない場合は `http://127.0.0.1:5000/` を入力します。黒いプログラム画面は終了まで閉じないでください。

## 2. macOS：ダウンロード、展開、起動、終了

1. Apple シリコンは macOS-arm64、Intel は macOS-x64 を v1.0.3 リリースページから選びます。
2. ZIP をダブルクリックし、展開されたフォルダー内のプログラムを起動します。
3. 開けない場合は右クリックして「開く」を選びます。それでも止められた場合は「システム設定 → プライバシーとセキュリティ → このまま開く」を選び、もう一度右クリックして「開く」を選びます。
4. 「開くアプリケーションがありません」と表示された場合は、第 16 節のターミナル方法を使います。実行中はターミナルを閉じないでください。

## 3. Linux：ダウンロード、展開、起動、終了

1. v1.0.3 リリースページから Linux-x64 の ZIP をダウンロードします。
2. 「ここに展開」を選び、展開したフォルダーでターミナルを開いて次を実行します。

```bash
chmod +x AttendanceSystem-*-Linux-x64
./AttendanceSystem-*-Linux-x64
```

3. ブラウザが開かない場合は `http://127.0.0.1:5000/` を入力します。終了時はターミナルで `Ctrl+C` を押します。

従業員、シフト、打刻記録は自動的に保存されます。配布ファイルには必要な実行環境が含まれています。

## 4. 初回設定

1. `http://127.0.0.1:5000/admin/` を開きます。
2. 「従業員管理」を開きます。
3. 従業員を登録します。
4. 勤務時間を設定します。
5. 必要に応じてシフトを作成します。

## 5. 従業員管理

「管理 → 従業員管理」で従業員の追加、編集、削除ができます。社員番号、氏名、区分または部署、使用言語を登録できます。従業員の横にあるシフトボタンから勤務時間を設定します。

従業員を削除する前に、内容をよく確認してください。削除した従業員は打刻できなくなります。

## 6. 出勤・退勤の打刻

1. 打刻画面で自分の名前を選びます。
2. 仕事を始めるときに「出勤打刻」を押します。
3. 仕事を終えるときに「退勤打刻」を押します。

同時に複数人が打刻した場合は、順番に処理されるまで待ってください。退勤後、勤務時間と勤怠状態が自動的に計算されます。

## 7. 希望勤務日の提出

希望提出画面を開き、名前を選び、各日を「出勤」「休み」「どちらでも可」に設定して保存します。

## 8. シフトの作成

「管理 → 自動シフト」で日ごとの必要人数を設定し、シフトを作成します。結果を確認し、必要に応じて調整してください。シフトは遅刻、早退、欠勤の判定に使われます。

## 9. 打刻記録の追加・修正

「管理 → 打刻修正」で従業員と日付を選び、記録を検索します。出勤時刻または退勤時刻を修正して保存します。記録がない日でも、時刻を入力して保存すると新しい記録を作成できます。

## 10. 休暇の登録

打刻修正画面で従業員と日付を選び、「休暇として登録」を選んで保存します。休暇は集計に表示されますが、欠勤には数えられません。

## 11. 勤怠集計の確認

「管理 → 勤怠集計」で従業員と期間を選びます。出勤日、予定日、勤務時間、遅刻、早退、欠勤、休暇を確認できます。結果は表形式のファイルとして出力できます。

## 12. スマートフォンで打刻

パソコンとスマートフォンを同じネットワークに接続します。管理画面で「打刻QRコード」を開き、スマートフォンで読み取ります。掲示する場合は印刷機能を使ってください。

## 13. 遅刻メール通知の設定

「管理 → メール設定」でメールサーバー、ポート、送信アカウント、アプリパスワード、管理者の受信アドレス、通知までの時間を入力します。メール設定は任意で、未設定でもほかの機能は使えます。

## 14. 表示言語の切り替え

管理画面上部の中文、English、日本語から選びます。選択した言語は保存されます。

## 15. 勤怠の計算方法

- 勤務時間は退勤時刻から出勤時刻を引き、重なった休憩時間を差し引きます。
- 始業時刻と猶予時間を過ぎた出勤は遅刻です。
- 終業時刻より前の退勤は早退です。
- シフトがある日に打刻記録がなければ欠勤です。
- 登録済みの休暇は欠勤に数えません。
- 退勤打刻がない記録は未完了として表示されます。

## 16. よくある問題

### ページが自動的に開かない

数秒待ってから、ブラウザに `http://127.0.0.1:5000` と入力してください。

### 従業員が打刻画面に表示されない

従業員が正しく登録されていることを確認し、打刻画面を更新してください。

### システムがすでに起動していると表示される

プログラムはすでに開いています。プログラムのウィンドウを確認し、ブラウザで `http://127.0.0.1:5000` を開いてください。

### 集計に欠勤が表示される

その日のシフトと打刻記録を確認し、必要であれば打刻修正画面で直してください。

### 退勤打刻を忘れた

管理者が打刻修正画面で退勤時刻を追加できます。

## 17. 打刻画面を選ぶ（最新ソース）

実行元のパソコンで `http://127.0.0.1:5000/admin/appearance` を開くか、管理画面の「画面スタイル」を選びます。

1. 「プレビュー・打刻体験」でデモ従業員を選び、成功アニメーションを試します。全体の設定や実際の勤怠記録は変更されません。
2. 管理画面に戻り「適用」を押します。「使用中」のスタイルは全体に適用され、従業員別の設定ではありません。
3. 「前のスタイルに戻す」で外観を戻せます。勤怠データは戻りません。
4. 上部の中国語・英語・日本語を切り替えると、名前、説明、ボタン、通知、プレビューも切り替わります。

| スタイル | 成功アニメーション |
| --- | --- |
| クラシック原版 | 確認のきらめき |
| ネオンSF | エネルギーリング |
| 着物カートゥーン | 少女のお辞儀 |
| オーロラの旅 | 流れ星 |
| 森の息吹 | 新芽の広がり |
| 深海ブルー | 水滴の波紋 |
| 山の夜明け | 日の出の光 |
| 今日のチケット | 打刻スタンプ |
| ピクセルアーケード | 跳ねるコイン |
| キャンディバブル | 紙吹雪 |
| ミニマルモノクロ | チェックマーク |

打刻 URL と QR コードは変わりません。新しく開く画面は現在の設定を使います。開いている画面は操作待ちの間、約 8 秒ごとに変更を確認し、素材の準備後に再読み込みします。従業員の選択を保持し、当日の状態をサーバーで再確認します。送信中、結果の確認中、成功ダイアログ表示中は切り替えを待ちます。再読み込みなしの即時切り替えではありません。

実際の打刻ではサーバーが成功を確認してからアニメーションを再生します。閉じる操作や再生の繰り返しでは打刻を追加しません。接続が切れた場合は結果確認を待ってください。3D 素材が使えない場合は簡易表示となり、動きを減らす設定にも対応します。

ページの期限切れや別ウィンドウでの変更が表示された場合は、管理画面を再読み込みしてください。スマートフォンでは選択済みの画面で打刻できますが、スタイル管理は実行元のパソコンに限定されます。

## 18. データ・バックアップ・デモ記録

| OS | 標準データディレクトリ |
| --- | --- |
| Windows | `%LOCALAPPDATA%\AttendanceSystem` |
| macOS | `~/Library/Application Support/AttendanceSystem` |
| Linux | `~/.local/share/attendance-system` |

`attendance.db`、アプリの秘密鍵、ログを保存します。`ATTENDANCE_DATA_DIR` が設定されている場合は、その場所を使用します。

バックアップはアプリを終了してからディレクトリ全体を別の場所にコピーします。復元前に現在のデータも保存し、終了状態で同じアプリバージョンのバックアップを復元して再起動してください。ソースのバックアップには実行時データは含まれません。データベースと秘密鍵は公開リポジトリにアップロードしないでください。

新規インストールは空のデータベースで始まります。スタイルのプレビューはデモ従業員を使用します。一部のローカルデモには「模拟」と記された架空の過去記録がありますが、実際の出勤ではなく、GitHub のソースや配布ファイルにも含まれません。集計ではデモ部署と実際の従業員を区別してください。利用者向けの 2 か月分データ一括生成機能はありません。

## 19. 接続先とシフトの注意点

- 管理画面は `http://127.0.0.1:5000/admin/` です。通常起動とローカルショートカットは `5000` を使い、初期デモの `5077` は使いません。ブラウザのタブを閉じても停止しません。プログラムのウィンドウを閉じると停止します。
- スマートフォンの `127.0.0.1` はスマートフォン自身です。QR コードまたは実行元パソコンの LAN IP とポート `5000` を使い、パソコンを起動したままファイアウォールで接続を許可してください。
- **勤怠判定は現在、従業員の曜日別勤務時間表を参照します。** 自動生成された日別シフトを直接参照しません。自動シフト変更後も曜日別の勤務時間・休日を確認してください。
- 標準の昼休みは 12:00–13:00、遅刻の猶予は 30 分です。片方の打刻がないと勤務時間を完全には計算できません。終業時刻以降の滞在時間が承認済み残業になるわけではありません。
- 完全な管理者アカウント・権限機能は未実装です。スタイル管理のローカル制限は、管理画面全体へのログイン保護ではありません。
