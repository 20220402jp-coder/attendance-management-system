<h1 align="center">📖 考勤打卡系统使用说明</h1>

<h2 align="center"><a href="README.md">返回下载页面</a></h2>

这份说明介绍系统中各项功能的使用方法。第一次使用时，建议按照下面的顺序完成设置。

## 1. 启动和关闭

1. 双击已经解压的程序。
2. 稍等片刻，系统会自动打开浏览器。
3. 使用结束后，关闭程序窗口即可停止系统。

系统中的员工、排班和打卡记录会自动保存，下次启动时仍然存在。

## 2. 第一次使用

首次启动后系统中没有员工，请先完成以下操作：

1. 打开 `http://127.0.0.1:5000/admin/` 进入管理后台。
2. 打开「员工管理」。
3. 添加员工。
4. 为员工设置工作时间。
5. 根据需要设置排班。

完成后，员工就可以在打卡页面选择自己的名字并打卡。

## 3. 员工管理

打开「管理 → 员工管理」，可以添加、修改或删除员工。

添加员工时可以填写：

- 工号
- 姓名
- 部门
- 使用语言

点击员工旁边的「排班表」，可以设置这名员工每天的上班和下班时间。

删除员工前请仔细确认。删除后，这名员工将不能继续打卡。

## 4. 员工上下班打卡

员工在打卡首页完成操作：

1. 选择自己的名字。
2. 上班时点击「签到上班」。
3. 下班时点击「签退下班」。

多人同时打卡时，系统会自动按顺序处理。如果按钮显示「排队中」，请等待处理完成，不要反复点击。

员工签退后，系统会自动计算当天的工作时间，并判断是否迟到或早退。

## 5. 员工提交希望上班的日期

打开「智能报班系统」页面：

1. 选择自己的名字。
2. 在日历中点击日期。
3. 为每天选择「上班」「休息」或「都可以」。
4. 确认后点击保存。

管理员自动排班时会参考员工提交的选择。

## 6. 管理员安排班表

打开「管理 → 自动排班」：

1. 设置每天需要上班的人数。
2. 确认员工已经提交希望上班的日期。
3. 点击自动排班。
4. 检查生成的排班结果。
5. 如果需要，可以手动调整班表。

自动排班生成逐日人员安排；当前考勤判定仍读取员工的每周工作时间表。请同时核对两处设置，详见第 17 节。

## 7. 修改或补充打卡记录

员工忘记打卡或时间记录不正确时，打开「管理 → 打卡修正」：

1. 选择员工。
2. 选择需要修改的日期。
3. 点击「查询」。
4. 修改签到时间或签退时间。
5. 点击「保存」。

如果当天没有记录，填写时间后保存即可补充一条记录。

## 8. 登记请假

打开「管理 → 打卡修正」：

1. 选择员工和请假日期。
2. 点击「查询」。
3. 勾选「标记为请假」。
4. 点击「保存」。

请假会显示在统计中，但不会算作缺勤。

## 9. 查看考勤统计

打开「管理 → 统计报表」：

1. 选择员工。
2. 选择开始日期和结束日期。
3. 查询考勤结果。

统计页面会显示：

- 出勤天数和排班天数
- 总工作时间和平均工作时间
- 迟到次数
- 早退次数
- 缺勤次数
- 请假次数

需要保存或交给其他人时，可以导出表格文件。

## 10. 使用手机打卡

电脑和手机需要连接同一个网络。

1. 在管理首页点击「打卡二维码」。
2. 让员工使用手机扫描二维码。
3. 手机会打开打卡页面，可以像在电脑上一样打卡。

需要张贴二维码时，可以使用页面中的打印功能，并选择保存为 PDF 后打印。

## 11. 设置迟到邮件提醒

打开「管理 → 邮件配置」，填写：

- 邮件服务器和端口
- 发件邮箱账号
- 邮箱密码或授权码
- 管理员收件邮箱
- 迟到多少分钟后发送提醒

填写后点击保存。不同邮箱的设置方法不同，一般需要在邮箱设置中开启发信功能并取得授权码。

不需要邮件提醒时，可以不填写这些内容，其他功能仍然可以正常使用。

## 12. 切换显示语言

进入管理后台后，在页面顶部选择中文、English 或日本語。这个开关会显示在所有管理页面上。

切换后，系统会记住当前选择。

## 13. 系统如何计算考勤

- 实际工作时间是签退时间减去签到时间，再扣除与午休重合的时间。
- 签到时间晚于规定上班时间和宽限时间，会显示为迟到。
- 签退时间早于规定下班时间，会显示为早退。
- 有排班但没有打卡记录的日期，会显示为缺勤。
- 已登记请假的日期，会显示为请假，不计入缺勤。
- 只有签到、没有签退的记录，会显示为未签退。

## 14. 常见问题

### 双击后没有自动打开页面

先等待几秒。如果仍未打开，可以在浏览器地址栏输入：

```text
http://127.0.0.1:5000
```

### 页面提示系统已经在运行

说明程序可能已经打开。请检查电脑上是否已有程序窗口，然后直接打开浏览器中的系统页面。

### 员工没有出现在打卡页面

请进入「管理 → 员工管理」，确认员工已经添加成功，然后刷新打卡页面。

### 统计结果出现缺勤

请检查当天是否安排了班表，以及员工是否有完整的签到和签退记录。如记录有误，可以在「打卡修正」中修改。

### 忘记签退

管理员可以进入「打卡修正」，选择员工和日期，补充签退时间后保存。

## 15. 选择打卡页面样式（最新源码）

在运行程序的电脑上打开 `http://127.0.0.1:5000/admin/appearance`，或点击后台的「页面样式」。

1. 点击「预览 · 试打卡」，选择演示员工，体验签到及成功动画。预览不会改变全局样式，也不会写入正式考勤。
2. 回到管理页点击「应用」，被选中的款式显示「使用中」。设置对整个系统生效，不是按员工单独设置。
3. 需要撤回时，点击「恢复上一样式」。它只恢复外观，不回退考勤数据。
4. 顶部可切换中文、English、日本語，样式名称、说明、按钮、提示和预览语言会一起变化。

| 样式 | 打卡成功动画 |
| --- | --- |
| 经典原版 | 确认光点 |
| 霓虹科幻 | 能量环确认 |
| 和服卡通 | 少女鞠躬 |
| 极光漫游 | 流星划过 |
| 森间呼吸 | 新芽舒展 |
| 深海蓝调 | 水滴涟漪 |
| 山间日出 | 日出光芒 |
| 今日票根 | 打卡盖章 |
| 像素街机 | 金币跳跃 |
| 糖果气泡 | 彩纸绽放 |
| 极简黑白 | 勾线绘制 |

员工打卡地址和二维码保持不变。新打开的页面直接使用新样式；已打开且空闲的页面约每 8 秒检查一次，在资源准备好后刷新切换并保留员工选择，再向服务器查询当天状态。提交中、结果待核实或成功弹窗未关闭时，会延后切换。此处采用安全时机刷新，并非无刷新的即时换肤。

正式页面只有在服务器确认打卡成功后才播放成功动画；可关闭或重播，重播不会再次打卡。网络中断时请等待结果核实，不要重复操作。三维资源无法使用时会降级显示，系统也支持减少动态效果。

出现「页面已过期」或「样式已被其他窗口修改」时，刷新管理页后重试。手机端可以使用所选样式打卡，但不能通过局域网地址管理样式。

## 16. 数据位置、备份和模拟数据

| 系统 | 默认数据目录 |
| --- | --- |
| Windows | `%LOCALAPPDATA%\AttendanceSystem` |
| macOS | `~/Library/Application Support/AttendanceSystem` |
| Linux | `~/.local/share/attendance-system` |

目录内包含数据库 `attendance.db`、程序密钥和日志。若配置了 `ATTENDANCE_DATA_DIR`，以该自定义目录为准。

备份时先关闭程序，再复制整个数据目录到其他位置；恢复前先备份现有目录，然后在程序关闭状态下还原同一应用版本的数据目录并重新启动。只备份源代码不包含这些运行数据。密钥和数据库应妥善保管，不要上传到公开仓库。

新安装默认为空数据库，不自动生成员工。后台的样式预览仅使用模拟员工。某些本地演示环境另有带「模拟」标识的历史数据，这些不是实际出勤记录，也不会随 GitHub 源码或下载包提供；统计时请区分模拟部门和实际员工。当前没有面向用户的一键生成两个月数据功能。

## 17. 入口和排班注意事项

- 管理入口是 `http://127.0.0.1:5000/admin/`，正式启动不用早期演示的 `5077`。快捷方式启动后也使用 `5000`；关闭浏览器标签页不会停止服务，关闭程序窗口才会停止。
- 手机上的 `127.0.0.1` 指手机自己。请扫描管理页的二维码，或使用运行程序电脑的局域网 IP 与 `5000` 端口；电脑需保持开机且防火墙允许访问。
- **考勤判定当前读取员工每周工作时间表**，并不直接读取自动排班生成的逐日班表。调整自动排班后，仍需核对员工每周的工作时间和休息日。
- 默认午休为 12:00–13:00，迟到宽限为 30 分钟；缺少任一打卡时间时工时不能完整计算。延后签退产生的在场时长不等同于已审批加班。
- 尚未实现完整管理员账号和权限管理。样式管理的本机访问限制不等于整个后台都有登录保护。
